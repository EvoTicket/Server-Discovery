name: Deploy to VPS with Docker

on:
  push:
    branches:
      - 'release*'
      - 'release/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.extract_info.outputs.version }}
      image_tag: ${{ steps.extract_info.outputs.image_tag }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Debug docker vars
        run: |
          echo "USERNAME=${{ secrets.DOCKER_USERNAME }}"
          echo "IMAGE_NAME=${{ secrets.DOCKER_IMAGE_NAME }}"
          echo "IMAGE_TAG=${{ steps.extract_info.outputs.image_tag }}"

      - name: Extract branch name and version
        id: extract_info
        run: |
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          echo "branch=$BRANCH_NAME" >> $GITHUB_OUTPUT
          
          # Tạo version từ tên nhánh (ví dụ: release/v1.0.0 -> v1.0.0)
          if [[ $BRANCH_NAME == release/* ]]; then
            VERSION=${BRANCH_NAME#release/}
          else
            # Nếu chỉ là "release" hoặc "release-xxx", dùng timestamp
            VERSION="v$(date +'%Y.%m.%d-%H%M%S')"
          fi
          
          # Tạo image tag (loại bỏ ký tự đặc biệt không hợp lệ cho Docker tag)
          IMAGE_TAG=$(echo "$VERSION" | tr '/' '-')
          
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "📦 Version: $VERSION"
          echo "🏷️ Image Tag: $IMAGE_TAG"

      - name: Check if tag exists
        id: check_tag
        run: |
          VERSION="${{ steps.extract_info.outputs.version }}"
          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "⚠️ Tag $VERSION already exists"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "✅ Tag $VERSION does not exist"
          fi

      - name: Create and push tag
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          VERSION="${{ steps.extract_info.outputs.version }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git tag -a "$VERSION" -m "Release $VERSION from ${{ steps.extract_info.outputs.branch }}"
          git push origin "$VERSION"
          
          echo "🏷️ Tag $VERSION created and pushed"

      - name: Generate release notes
        id: release_notes
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          VERSION="${{ steps.extract_info.outputs.version }}"
          BRANCH="${{ steps.extract_info.outputs.branch }}"
          
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          
          if [ -z "$PREVIOUS_TAG" ]; then
            COMMITS=$(git log --pretty=format:"- %s (%h)" -10)
          else
            COMMITS=$(git log $PREVIOUS_TAG..HEAD --pretty=format:"- %s (%h)")
          fi
          
          cat << EOF > release_notes.md
          ## 🚀 Release $VERSION
          
          **Branch:** \`$BRANCH\`
          **Date:** $(date +'%Y-%m-%d %H:%M:%S')
          **Docker Image:** \`${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ steps.extract_info.outputs.image_tag }}\`
          
          ### 📝 Changes
          $COMMITS
          
          ### 🔗 Links
          - [View diff](https://github.com/${{ github.repository }}/compare/$PREVIOUS_TAG...$VERSION)
          - [Docker Hub](https://hub.docker.com/r/${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }})
          EOF

      - name: Create GitHub Release
        if: steps.check_tag.outputs.exists == 'false'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.extract_info.outputs.version }}
          name: Release ${{ steps.extract_info.outputs.version }}
          body_path: release_notes.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ─────────────────────────────────────────
      # SONARQUBE ANALYSIS & QUALITY GATE
      # ─────────────────────────────────────────
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Grant permission
        run: chmod +x gradlew

      - name: Cache Gradle & SonarQube packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ~/.sonar/cache
          key: ${{ runner.os }}-gradle-sonar-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-sonar-

      - name: Build & SonarQube Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          ./gradlew build sonar -x test \
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
            -Dsonar.token=${{ secrets.SONAR_TOKEN }} \
            -Dsonar.qualitygate.wait=true \
            --info

      - name: Verify custom quality conditions
        run: |
          echo "🔍 Checking SonarQube metrics..."

          METRICS=$(curl -s -u "${{ secrets.SONAR_TOKEN }}:" \
            "${{ secrets.SONAR_HOST_URL }}/api/measures/component?component=${{ secrets.SONAR_PROJECT_KEY }}&metricKeys=security_rating,reliability_rating,sqale_rating,blocker_violations")

          SECURITY=$(echo "$METRICS"        | jq -r '.component.measures[] | select(.metric=="security_rating")    | .value')
          RELIABILITY=$(echo "$METRICS"     | jq -r '.component.measures[] | select(.metric=="reliability_rating") | .value')
          MAINTAINABILITY=$(echo "$METRICS" | jq -r '.component.measures[] | select(.metric=="sqale_rating")       | .value')
          BLOCKERS=$(echo "$METRICS"        | jq -r '.component.measures[] | select(.metric=="blocker_violations")  | .value')

          echo "Security rating:        $SECURITY (1=A)"
          echo "Reliability rating:     $RELIABILITY (1=A)"
          echo "Maintainability rating: $MAINTAINABILITY (1=A)"
          echo "Blocker violations:     $BLOCKERS"

          FAILED=false
          [ "$SECURITY"        != "1.0" ] && echo "❌ Security must be A (got $SECURITY)"               && FAILED=true
          [ "$RELIABILITY"     != "1.0" ] && echo "❌ Reliability must be A (got $RELIABILITY)"         && FAILED=true
          [ "$MAINTAINABILITY" != "1.0" ] && echo "❌ Maintainability must be A (got $MAINTAINABILITY)" && FAILED=true
          [ "$BLOCKERS"        != "0" ] && echo "❌ Blocker violations must be 0 (got $BLOCKERS)"      && FAILED=true

          if [ "$FAILED" = "true" ]; then
            echo "🚫 Quality gate failed — blocking deployment"
            exit 1
          fi

          echo "✅ All quality conditions passed!"
          echo "   Security: A | Reliability: A | Maintainability: A | Blockers: 0"

      - name: Notify Slack on SonarQube failure
        if: failure()
        run: |
          curl -s -X POST -H 'Content-type: application/json' \
          --data "{
            \"attachments\": [{
              \"color\": \"danger\",
              \"title\": \"🔴 SonarQube Quality Gate FAILED - INVENTORY SERVICE\",
              \"fields\": [
                {\"title\": \"Version\",   \"value\": \"${{ steps.extract_info.outputs.version }}\", \"short\": true},
                {\"title\": \"Branch\",    \"value\": \"${{ steps.extract_info.outputs.branch }}\",  \"short\": true},
                {\"title\": \"Reason\",    \"value\": \"Security/Reliability/Maintainability phải đạt A và Blocker = 0\", \"short\": false},
                {\"title\": \"SonarQube\", \"value\": \"${{ secrets.SONAR_HOST_URL }}/dashboard?id=${{ secrets.SONAR_PROJECT_KEY }}\", \"short\": false}
              ],
              \"footer\": \"GitHub Actions\",
              \"ts\": $(date +%s)
            }]
          }" \
          ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ steps.extract_info.outputs.image_tag }}
            ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:latest
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:buildcache,mode=max

  deploy-to-vps:
    runs-on: ubuntu-latest
    needs: build-and-push
    
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            cd ${{ secrets.VPS_DEPLOY_PATH || '~/app' }}
            
            # Backup file .env hiện tại (nếu có)
            if [ -f .env ]; then
              cp .env .env.backup
            fi
            
            # Update image tag trong docker-compose.yml
            export IMAGE_TAG=${{ needs.build-and-push.outputs.image_tag }}
            export DOCKER_IMAGE=${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}
            
            # Pull image mới nhất
            docker pull ${DOCKER_IMAGE}:${IMAGE_TAG}
            
            # Stop và remove container cũ
            docker-compose down
            
            # Start với image mới
            docker-compose up -d
            
            # Clean up old images (giữ lại 3 images gần nhất)
            docker image prune -af --filter "until=72h"
            
            echo "✅ Deployment completed successfully!"
            echo "🐳 Image: ${DOCKER_IMAGE}:${IMAGE_TAG}"

      - name: Deployment Summary
        if: success()
        run: |
          echo "### ✅ Deployment Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Version:** ${{ needs.build-and-push.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Docker Image:** \`${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** Deployed to VPS" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "View logs: \`ssh ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} 'cd ${{ secrets.VPS_DEPLOY_PATH || '~/app' }} && docker-compose logs -f'\`" >> $GITHUB_STEP_SUMMARY
          
      - name: Notify Slack (Final)
        if: always()
        run: |
          STATUS="${{ job.status }}"
          VERSION="${{ needs.build-and-push.outputs.version }}"
          IMAGE="${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}"
      
          if [ "$STATUS" = "success" ]; then
            TITLE="🚀 Deployment Successful - SERVER DISCOVERY"
            COLOR="good"
          else
            TITLE="❌ Deployment Failed - SERVER DISCOVERY"
            COLOR="danger"
          fi
      
          curl -X POST -H 'Content-type: application/json' \
          --data "{
            \"attachments\": [
              {
                \"color\": \"$COLOR\",
                \"title\": \"$TITLE\",
                \"fields\": [
                  {\"title\": \"Version\", \"value\": \"$VERSION\", \"short\": true},
                  {\"title\": \"Image\", \"value\": \"$IMAGE\", \"short\": false},
                  {\"title\": \"Status\", \"value\": \"$STATUS\", \"short\": true}
                ],
                \"footer\": \"GitHub Actions\",
                \"ts\": $(date +%s)
              }
            ]
          }" \
          ${{ secrets.SLACK_WEBHOOK_URL }}
